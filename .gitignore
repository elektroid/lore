lore-engine
lore.toml
external-material/
lore.db
lore.db-shm
lore.db-wal

backend/lore.db
backend/lore.db-shm
backend/lore.db-wal
backend/backups/
backend/tmp/
backend/vendor/
backend/server
backend/data/uploads/

frontend/node_modules/
frontend/dist/

.DS_Store
.claude/

# Machine-local notes, never for git (e.g. docs/deploy-nobodix.local.md).
*.local.md

# The frontend is copied here by `make build` and embedded in the binary.
# Only the placeholder is tracked, so a source checkout still compiles.
backend/internal/web/dist/*
!backend/internal/web/dist/.gitkeep
